<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        Required so DailyRecommendationWorker can promote itself to a
        foreground service via setForeground() / getForegroundInfo().
        Without this the OS will silently kill the worker process when
        the user backgrounds the app, and the "Send Today's Picks Now"
        scan disappears without a trace.

        NOTE: we deliberately do NOT declare FOREGROUND_SERVICE_DATA_SYNC
        (or any other fine-grained sub-permission). The 2-arg
        ForegroundInfo(id, notification) constructor lets WorkManager
        pick a compatible default type, matching whatever its bundled
        SystemForegroundService declares in its own manifest. Requesting
        a specific type when the SystemForegroundService doesn't declare
        it produces a hard IllegalArgumentException that crashes the app
        (2026-07-04 user report: "StockWiz AI closed because this app
        has a bug" after tapping Send Today's Picks Now).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceStreamAI">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:label="@string/app_name"
            android:theme="@style/Theme.FinanceStreamAI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>