<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        Required so DailyRecommendationWorker can promote itself to a
        foreground service via setForeground() / getForegroundInfo().
        Without these the OS will silently kill the worker process when
        the user backgrounds the app, and the "Send Today's Picks Now"
        scan disappears without a trace.

        FOREGROUND_SERVICE_DATA_SYNC is a fine-grained sub-permission
        required by Android 14+ (API 34) for any foreground service that
        performs network sync work. WorkManager 2.9+ ties its expedited
        worker foreground services to this type when the worker declares
        foregroundServiceType = FOREGROUND_SERVICE_TYPE_DATA_SYNC.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceStreamAI">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:label="@string/app_name"
            android:theme="@style/Theme.FinanceStreamAI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>